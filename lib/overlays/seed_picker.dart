import 'package:flutter/material.dart';

class SeedPicker extends StatelessWidget {
  const SeedPicker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Select a game number (1 - 1 000 000)');
  }
}
